language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t asong91/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true asong91/docker-react npm run test -- --coverage